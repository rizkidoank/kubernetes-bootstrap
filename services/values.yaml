argocd:
  cluster: https://kubernetes.default.svc
  project: default

jenkins:
  enabled: true
  namespace: default
  version: 8.0.10
  values: |
    persistence:
      enabled: false
    jenkinsUser: rizki
